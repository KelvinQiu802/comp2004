package model;

public class SingleColorProperty extends PropertyCard{

    public SingleColorProperty(String name, int value, String description,Colors currentColor) {
        super(name, value, description, currentColor);
    }
}
